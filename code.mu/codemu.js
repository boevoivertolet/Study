"use strict"


let num = 123453;
let last = String(num)[String(num).length -1];

	if (last == 0 || last == 2 || last == 4 || last == 6 || last == 8) {
	console.log('чет');
	} else {
	console.log('нечет');
	}

